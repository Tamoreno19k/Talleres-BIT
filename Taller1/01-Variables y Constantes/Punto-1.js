/* Crea un programa que declare una variable para almacenar la temperatura actual en grados Celsius. 
* Luego, convierte esta temperatura a grados Fahrenheit utilizando la fórmula correspondiente y muestra el
* resultado en la consola
*/

const 
    tempActCel = 11, 
    tempActFah = ((tempActCel * 1.8)+ 32);

console.log (tempActFah);
