/* Declara una constante llamada PI y asigna el valor de π (aproximadamente 3.14159) a ella. Luego, 
pide al usuario que ingrese el radio de un círculo y calcula su área utilizando la constante PI.
*/

let radio = prompt('Digita el radio de un circulo en CM');

const 
    PI = 3.14159,
    Area = (PI * (radio **2));

    radio = Number(radio);

    console.log (Area);